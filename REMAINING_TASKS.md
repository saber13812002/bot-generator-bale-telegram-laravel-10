# بخش‌های باقی‌مانده از پلان

بررسی پلان نشان می‌دهد که موارد زیر هنوز پیاده‌سازی نشده‌اند:

## ✅ بخش‌های انجام شده

1. ✅ ساخت جدول `ai_llms` با migration و model
2. ✅ اضافه کردن فیلد `ai_id` به جدول `missions`
3. ✅ اضافه کردن فیلد `selected_ai_id` به جدول `mission_personnel`
4. ✅ به‌روزرسانی Model Mission برای relationship با AiLlm
5. ✅ به‌روزرسانی Model MissionPersonnel برای relationship با AiLlm
6. ✅ تغییر روش ارسال Prompt و Content به صورت خالص در MissionBotController
7. ✅ اضافه کردن UI انتخاب AI در MissionBotController
8. ✅ اضافه کردن دستورات مدیریت AI در PersonnelAdminBotController (فقط `/add_ai` و `/list_ai`)
9. ✅ به‌روزرسانی SendMissionMediaJob برای ارسال خالص Prompt و Content
10. ✅ ساخت Seeder برای داده‌های اولیه AI

## ❌ بخش‌های باقی‌مانده

### 1. Nova Resource برای AiLlm (بخش 9)

**فایل مورد نیاز:** `app/Nova/AiLlm.php`

**توضیحات:** 
- مدیریت AI از طریق Nova Admin Panel
- امکان ویرایش URL و توضیحات
- امکان فعال/غیرفعال کردن AI

**اولویت:** متوسط (می‌توان از طریق Nova هم مدیریت کرد)

---

### 2. دستورات کامل مدیریت AI در ربات ادمین (بخش 6)

**دستورات باقی‌مانده:**
- `/edit_ai [id] [نام]` - ویرایش AI
- `/delete_ai [id]` - حذف AI (soft delete)

**توضیحات:**
- در حال حاضر فقط `/add_ai` و `/list_ai` پیاده‌سازی شده
- طبق پلان: "فقط اضافه کردن، نه ویرایش/حذف" - پس این بخش اختیاری است

**اولویت:** پایین (طبق پلان فقط اضافه کردن کافی است)

---

### 3. Repository و Service Pattern (بخش 8)

**فایل‌های مورد نیاز:**
- `app/Interfaces/Repositories/AiLlmRepository.php`
- `app/Repositories/AiLlmRepositoryImpl.php`
- `app/Interfaces/Services/AiLlmService.php`
- `app/Services/AiLlmServiceImpl.php`

**توضیحات:**
- در پلان به عنوان "اختیاری" ذکر شده
- در حال حاضر منطق مستقیماً در Controller است

**اولویت:** پایین (اختیاری است)

---

### 4. API با توکن برای مدیریت Mission/Content/Prompt (بخش در فایل‌های مورد نیاز)

**توضیحات از پلان:**
> "اگر بتوان این ای پی آی ها که به این کنترل ها متصل هست را از طریق یک کلید استاتیک مدیریت کرد یا برای هر تننت توکن ساخته شود که از طریق همان توکن بتوانند محتوا و پرامپت هایشان را در صف از طریق وب سرویس قرار دهند. یا من از طریق توکن سوپر ادمین خودم بتوانم در هر تننت اطلاعات خودشان را با وب سرویس اضافه کنم"

**نیازمندی‌ها:**
- سیستم توکن برای هر Tenant
- API endpoints برای:
  - اضافه کردن Mission
  - اضافه کردن Content
  - اضافه کردن Prompt
- توکن Super Admin برای دسترسی به همه Tenants
- توکن Tenant برای دسترسی فقط به Tenant خودش

**اولویت:** بالا (اگر نیاز به API باشد)

---

## 📊 خلاصه اولویت‌ها

### اولویت بالا:
1. **API با توکن** (اگر نیاز به API باشد)

### اولویت متوسط:
2. **Nova Resource برای AiLlm** (برای مدیریت بهتر از طریق پنل)

### اولویت پایین:
3. **Repository و Service Pattern** (اختیاری - برای بهبود معماری)
4. **دستورات edit/delete در ربات ادمین** (اختیاری - طبق پلان فقط add کافی است)

---

## 💡 توصیه

برای استفاده کامل از سیستم، پیشنهاد می‌شود:

1. **Nova Resource** را پیاده‌سازی کنید (برای مدیریت URL و توضیحات AI)
2. **API با توکن** را در صورت نیاز به وب‌سرویس پیاده‌سازی کنید

بخش‌های دیگر اختیاری هستند و می‌توانند در آینده اضافه شوند.

