# راهنمای تست سیستم انتخاب ماموریت و تگ‌ها

این راهنما مراحل تست کامل سیستم انتخاب ماموریت با فیلترها و تگ‌ها را شرح می‌دهد.

## پیش‌نیازها

1. اجرای migration:
```bash
php artisan migrate
```

2. اطمینان از وجود داده‌های تست:
   - حداقل یک Tenant
   - حداقل یک Personnel
   - حداقل چند Mission با duration و points مختلف
   - حداقل چند Tag

## تست 1: Migration و Database

### 1.1 بررسی فیلد duration

```sql
-- بررسی وجود فیلد duration
DESCRIBE missions;

-- اضافه کردن داده تست
UPDATE missions SET duration = 1 WHERE id = 1;
UPDATE missions SET duration = 5 WHERE id = 2;
UPDATE missions SET points = 10 WHERE id = 1;
UPDATE missions SET points = 15 WHERE id = 2;
```

### 1.2 بررسی تگ‌ها

```sql
-- بررسی وجود تگ‌ها
SELECT * FROM tags LIMIT 5;

-- اتصال تگ به ماموریت
INSERT INTO taggables (tag_id, taggable_type, taggable_id) 
VALUES (1, 'App\\Models\\Mission', 1);
```

## تست 2: ربات مدیا - لیست ماموریت‌ها

### 2.1 تست دستور /list_missions

1. در ربات مدیا دستور زیر را ارسال کنید:
   ```
   /list_missions
   ```

2. باید لیست متنی ماموریت‌ها با ID و عنوان نمایش داده شود.

### 2.2 تست دکمه دانلود

1. دستور زیر را ارسال کنید:
   ```
   /upload_mission_1
   ```

2. یک فایل (عکس، ویدیو، صوت یا PDF) ارسال کنید.

3. باید پیام تایید با دکمه "📥 دانلود" نمایش داده شود.

4. روی دکمه کلیک کنید.

5. باید لینک دانلود فایل ارسال شود.

## تست 3: ربات ماموریت - دکمه‌های انتخاب

### 3.1 تست دکمه ماموریت رندوم

1. در ربات ماموریت دستور `/start` را ارسال کنید.

2. باید دو دکمه نمایش داده شود:
   - 🎲 ماموریت رندوم
   - 🔍 انتخاب نوع ماموریت

3. روی دکمه "🎲 ماموریت رندوم" کلیک کنید.

4. باید یک ماموریت تصادفی اختصاص داده شود و اطلاعات آن نمایش داده شود.

5. باید لینک‌های آموزشی مرتبط (اگر وجود داشته باشد) ارسال شود.

### 3.2 تست فیلتر بر اساس Duration

1. روی دکمه "🔍 انتخاب نوع ماموریت" کلیک کنید.

2. روی دکمه "⏱️ ماموریت 1 دقیقه‌ای" کلیک کنید.

3. باید لیست ماموریت‌هایی با duration = 1 نمایش داده شود.

4. روی یکی از ماموریت‌ها کلیک کنید.

5. باید ماموریت اختصاص داده شود و اطلاعات آن نمایش داده شود.

### 3.3 تست فیلتر بر اساس Points

1. روی دکمه "🔍 انتخاب نوع ماموریت" کلیک کنید.

2. روی دکمه "🎯 امتیاز 10 یا بیشتر" کلیک کنید.

3. باید لیست ماموریت‌هایی با points >= 10 نمایش داده شود.

4. روی یکی از ماموریت‌ها کلیک کنید.

5. باید ماموریت اختصاص داده شود.

### 3.4 تست فیلتر بر اساس Tag

1. ابتدا یک تگ به یک ماموریت متصل کنید:
   ```sql
   INSERT INTO taggables (tag_id, taggable_type, taggable_id) 
   VALUES (1, 'App\\Models\\Mission', 1);
   ```

2. در ربات، روی دکمه "🔍 انتخاب نوع ماموریت" کلیک کنید.

3. روی دکمه "🏷️ بر اساس تگ" کلیک کنید.

4. باید لیست تگ‌ها نمایش داده شود.

5. روی یکی از تگ‌ها کلیک کنید.

6. باید لیست ماموریت‌هایی با آن تگ نمایش داده شود.

7. روی یکی از ماموریت‌ها کلیک کنید.

8. باید ماموریت اختصاص داده شود.

## تست 4: ارسال لینک‌های آموزشی

### 4.1 تست ارسال خودکار لینک‌ها

1. یک ماموریت با محتوای آموزشی ایجاد کنید:
   ```sql
   -- ایجاد محتوا
   INSERT INTO contents (tenant_id, title, content_type, content_url, description, sort_order)
   VALUES (1, 'آموزش تست', 'video', 'https://example.com/video.mp4', 'توضیحات', 1);

   -- اتصال به ماموریت
   INSERT INTO mission_contents (mission_id, content_id, sort_order)
   VALUES (1, 1, 1);
   ```

2. یک ماموریت را اختصاص دهید (از طریق دکمه رندوم یا فیلتر).

3. باید بعد از اختصاص ماموریت، لینک‌های آموزشی مرتبط ارسال شود.

## تست 5: بررسی خطاها

### 5.1 تست ماموریت پر

1. یک ماموریت با `max_personnel = 1` و `current_personnel_count = 1` ایجاد کنید.

2. سعی کنید آن را اختصاص دهید.

3. باید پیام خطا نمایش داده شود.

### 5.2 تست ماموریت فعال موجود

1. یک ماموریت را اختصاص دهید.

2. سعی کنید ماموریت دیگری را اختصاص دهید.

3. باید پیام "شما یک ماموریت فعال دارید" نمایش داده شود.

### 5.3 تست عدم وجود ماموریت

1. تمام ماموریت‌ها را غیرفعال کنید:
   ```sql
   UPDATE missions SET status = 'inactive';
   ```

2. سعی کنید ماموریت رندوم دریافت کنید.

3. باید پیام "در حال حاضر ماموریت در دسترس نیست" نمایش داده شود.

## تست 6: لاگ‌ها

بررسی لاگ‌ها برای اطمینان از عملکرد صحیح:

```bash
tail -f storage/logs/laravel.log | grep "Mission Bot"
```

باید لاگ‌های زیر را ببینید:
- `🔘 Mission Bot - Callback query received`
- `✅ Mission Bot - Mission requested successfully`
- `📚 Mission Bot - Training links sent`

## چک‌لیست نهایی

- [ ] Migration اجرا شده و فیلد duration اضافه شده
- [ ] دستور `/list_missions` در ربات مدیا کار می‌کند
- [ ] دکمه دانلود بعد از آپلود مدیا نمایش داده می‌شود
- [ ] دکمه‌های رندوم و فیلتر در `/start` نمایش داده می‌شوند
- [ ] ماموریت رندوم به درستی اختصاص داده می‌شود
- [ ] فیلتر بر اساس duration کار می‌کند
- [ ] فیلتر بر اساس points کار می‌کند
- [ ] فیلتر بر اساس tag کار می‌کند
- [ ] لینک‌های آموزشی بعد از اختصاص ماموریت ارسال می‌شوند
- [ ] خطاها به درستی مدیریت می‌شوند

## نکات مهم

1. **Callback Query Handling**: اطمینان حاصل کنید که webhook callback queries را دریافت می‌کند.

2. **Inline Keyboard**: دکمه‌های inline keyboard باید به درستی نمایش داده شوند.

3. **Database Transactions**: اطمینان حاصل کنید که اختصاص ماموریت در transaction انجام می‌شود.

4. **Error Handling**: تمام خطاها باید catch شده و پیام مناسب به کاربر نمایش داده شود.

5. **Logging**: تمام عملیات مهم باید لاگ شوند.

## عیب‌یابی

### مشکل: دکمه‌ها کار نمی‌کنند

- بررسی کنید که callback query handler در `index()` متد فراخوانی می‌شود
- بررسی کنید که `callback_query` در update وجود دارد

### مشکل: فیلترها نتیجه نمی‌دهند

- بررسی کنید که داده‌های تست در دیتابیس وجود دارند
- بررسی کنید که tenant_id درست است
- بررسی کنید که status ماموریت‌ها 'active' است

### مشکل: لینک‌های آموزشی ارسال نمی‌شوند

- بررسی کنید که محتواها به ماموریت متصل شده‌اند
- بررسی کنید که relationship `contents()` درست کار می‌کند

