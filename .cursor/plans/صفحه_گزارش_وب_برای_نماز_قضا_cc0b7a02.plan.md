---
name: صفحه گزارش وب برای نماز قضا
overview: ایجاد سیستم گزارش وب برای کاربران نماز قضا با token منحصر به فرد، به‌روزرسانی صفحه اصلی bots.pardisania.ir، و اضافه کردن لینک به ایمیل‌ها
todos:
  - id: migration_token
    content: ایجاد Migration برای اضافه کردن web_report_token به bot_users
    status: completed
  - id: migration_endpoints_links
    content: ایجاد Migration برای اضافه کردن فیلدهای لینک به webhook_endpoints
    status: completed
  - id: config_analytics
    content: ایجاد config/analytics.php برای تنظیمات مرکزی Analytics
    status: completed
  - id: layout_web
    content: ایجاد layout مشترک web.blade.php با Analytics scripts
    status: completed
    dependencies:
      - config_analytics
  - id: controller_web
    content: ایجاد PrayerReportWebController با متدهای show و test
    status: completed
    dependencies:
      - migration_token
  - id: routes_web
    content: اضافه کردن Route های /namaz-ghaza/{token} و /test-js
    status: completed
    dependencies:
      - controller_web
  - id: view_report
    content: ایجاد view prayer-report-web.blade.php با نمودارهای Chart.js (استفاده از Layout)
    status: completed
    dependencies:
      - controller_web
      - layout_web
  - id: view_test
    content: ایجاد view test-js.blade.php برای تست JavaScript (استفاده از Layout)
    status: completed
    dependencies:
      - routes_web
      - layout_web
  - id: update_welcome
    content: به‌روزرسانی welcome.blade.php با لیست کامل ربات‌ها از webhook_endpoints (استفاده از Layout)
    status: completed
    dependencies:
      - migration_endpoints_links
      - layout_web
  - id: generate_token
    content: اضافه کردن تولید web_report_token در PrayerBotController هنگام تایید ایمیل
    status: completed
    dependencies:
      - migration_token
  - id: update_email_views
    content: اضافه کردن چندین باکس HTML با توضیحات و لینک گزارش به view های ایمیل
    status: completed
    dependencies:
      - routes_web
  - id: update_email_data
    content: اضافه کردن reportUrl به EmailData::generateTextReport با چندین لینک
    status: completed
    dependencies:
      - generate_token
  - id: update_transform
    content: اضافه کردن report_url به transformReportData در Commands
    status: completed
    dependencies:
      - generate_token
---

# ایجاد صفحه گزارش وب برای نماز قضا

## اهداف

1. ایجاد صفحه گزارش وب با نمودارهای تعاملی برای هر کاربر
2. استفاده از token منحصر به فرد برای دسترسی امن
3. به‌روزرسانی صفحه اصلی bots.pardisania.ir
4. اضافه کردن لینک گزارش به ایمیل‌های هفتگی

## مراحل پیاده‌سازی

### 1. Migration و Model

- **Migration**: اضافه کردن فیلد `web_report_token` به جدول `bot_users`
  - فایل: `database/migrations/YYYY_MM_DD_add_web_report_token_to_bot_users_table.php`
  - فیلد: `string('web_report_token', 64)->nullable()->unique()->after('email_unsubscribe_token')`
  - Index: `$table->index('web_report_token');`
- **Model**: به‌روزرسانی `app/Models/BotUsers.php` برای cast کردن `web_report_token`

### 2. Controller

- **Controller جدید**: `app/Http/Controllers/PrayerReportWebController.php`
  - متد `show(string $token)`: نمایش صفحه گزارش با token
  - متد `test()`: صفحه تست JavaScript
  - دریافت داده‌های گزارش از `PrayerBotService::getWeeklyReport()`
  - تبدیل داده‌ها با `transformReportData()`
  - ارسال به view

### 3. Routes

- **Route در `routes/web.php`**:
  - `Route::get('/namaz-ghaza/{token}', [PrayerReportWebController::class, 'show']);` (URL انگلیسی برای کوتاه‌تر شدن)
  - `Route::get('/test-js', [PrayerReportWebController::class, 'test']);`

### 4. Views

- **View اصلی**: `resources/views/prayer-report-web.blade.php`
  - استفاده از Chart.js برای نمودارها
  - نمایش آمار 7 روز گذشته
  - مقایسه با هفته‌های گذشته
  - اطلاعات پیک فعالیت
  - آمار Top 10
  - پیام انگیزشی
  - طراحی Responsive و RTL
- **View تست**: `resources/views/test-js.blade.php`
  - تست لود شدن Chart.js
  - تست نمایش نمودار ساده

### 5. به‌روزرسانی Welcome Page

- **فایل**: `resources/views/welcome.blade.php`
  - خواندن لیست کامل ربات‌ها از جدول `webhook_endpoints` (فقط `is_active = true`)
  - نمایش هر ربات با:
    - نام و توضیحات
    - آیکون مناسب (SVG یا Emoji)
    - لینک به نمونه ربات (Telegram/Bale)
    - لینک به وبلاگ فارسی (ویرگول) - اختیاری
    - لینک به وبلاگ انگلیسی (Medium) - اختیاری
  - اضافه کردن فیلدهای `sample_telegram_link`, `sample_bale_link`, `blog_virgool_link`, `blog_medium_link` به جدول `webhook_endpoints` (Migration جدید)
  - استفاده از Layout مشترک برای تم واحد

### 6. تولید و مدیریت Token

- **در `PrayerBotController`**: هنگام تایید ایمیل، تولید `web_report_token` اگر وجود نداشته باشد
  - فایل: `app/Http/Controllers/PrayerBotController.php`
  - متد: `handleEmailVerificationCode()`
  - کد: `$botUser->web_report_token = bin2hex(random_bytes(32));`
- **در `SendPrayerWeeklyReports`**: اطمینان از وجود token برای همه کاربران

### 7. به‌روزرسانی ایمیل‌ها

- **View های ایمیل**: اضافه کردن چندین باکس HTML زیبا با لینک به صفحه گزارش
  - فایل‌ها: `resources/views/emails/prayer-weekly-report-v*.blade.php`
  - **باکس 1**: در ابتدای ایمیل - "📊 گزارش کامل شما آماده است! برای مشاهده نمودارهای تعاملی و آمار دقیق، اینجا کلیک کنید"
  - **باکس 2**: بعد از آمار کلی - "📈 می‌خواهید نمودار پیشرفت خود را ببینید؟ گزارش کامل در صفحه وب"
  - **باکس 3**: بعد از پیام انگیزشی - "🌟 برای مشاهده جزئیات بیشتر و مقایسه با هفته‌های گذشته، به صفحه گزارش مراجعه کنید"
  - هر باکس شامل:
    - توضیحات جذاب درباره محتوای گزارش
    - دکمه/لینک با طراحی زیبا
    - رنگ‌بندی مناسب و Responsive
  - لینک: `url('/namaz-ghaza/' . $user->web_report_token)`
- **EmailData**: اضافه کردن `reportUrl` به `generateTextReport()` با چندین لینک در متن

### 8. به‌روزرسانی transformReportData

- **در Commands**: اضافه کردن `report_url` به `reportData`
  - فایل‌ها: 
    - `app/Console/Commands/SendPrayerWeeklyReports.php`
    - `app/Console/Commands/TestWeeklyReportJob.php`
    - `app/Console/Commands/SendUserWeeklyReport.php`

### 9. تنظیمات Analytics مرکزی

- **Config File**: `config/analytics.php`
  - تنظیمات Google Analytics (GA4)
  - تنظیمات Microsoft Clarity
  - تنظیمات Hotjar
  - فعال/غیرفعال کردن هر سرویس
- **Layout مشترک**: `resources/views/layouts/web.blade.php`
  - شامل Analytics scripts
  - شامل Meta tags مشترک
  - شامل CSS/JS مشترک
  - استفاده در همه صفحات وب (welcome, prayer-report-web, test-js)

### 10. Migration برای Webhook Endpoints

- **Migration**: اضافه کردن فیلدهای لینک به `webhook_endpoints`
  - فایل: `database/migrations/YYYY_MM_DD_add_links_to_webhook_endpoints_table.php`
  - فیلدها:
    - `sample_telegram_link` (nullable string)
    - `sample_bale_link` (nullable string)
    - `blog_virgool_link` (nullable string)
    - `blog_medium_link` (nullable string)
    - `icon_emoji` (nullable string) - برای آیکون Emoji
    - `icon_svg` (nullable text) - برای آیکون SVG سفارشی

## ساختار فایل‌ها

```
database/migrations/
  ├── YYYY_MM_DD_add_web_report_token_to_bot_users_table.php
  └── YYYY_MM_DD_add_links_to_webhook_endpoints_table.php

config/
  └── analytics.php (جدید)

app/Http/Controllers/
  └── PrayerReportWebController.php

resources/views/
  ├── layouts/
  │   └── web.blade.php (Layout مشترک با Analytics)
  ├── prayer-report-web.blade.php
  ├── test-js.blade.php
  └── welcome.blade.php (به‌روزرسانی - استفاده از Layout)

routes/
  └── web.php (به‌روزرسانی)

resources/views/
  └── emails/
      └── prayer-weekly-report-v*.blade.php (به‌روزرسانی)
```

## نکات امنیتی

- Token باید 64 کاراکتر hex باشد
- Token باید unique باشد
- صفحه گزارش فقط با token قابل دسترسی است (بدون نیاز به لاگین)
- در صورت عدم وجود token یا کاربر، صفحه 404 نمایش داده شود

## تست

1. تست صفحه `/test-js` برای اطمینان از لود شدن JavaScript
2. تست تولید token برای کاربر جدید
3. تست دسترسی به صفحه گزارش با token معتبر
4. تست دسترسی با token نامعتبر (باید 404 شود)
5. تست نمایش نمودارها و داده‌ها
6. تست لینک در ایمیل