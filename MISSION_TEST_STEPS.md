# مراحل تست سیستم ماموریت‌ها - به ترتیب

## ✅ مرحله 1: آماده‌سازی دیتابیس

```bash
# اجرای migrations
php artisan migrate

# اجرای seeders پایه
php artisan db:seed --class=TenantSeeder
php artisan db:seed --class=PersonnelSeeder

# اجرای seeder تست ماموریت (این یک ماموریت کامل می‌سازد)
php artisan db:seed --class=MissionTestSeeder
```

**نتیجه**: یک ماموریت با ID مشخص ایجاد می‌شود که به یک یا دو پرسنل assign شده است.

---

## ✅ مرحله 2: تنظیمات Environment

در فایل `.env` این متغیرها را تنظیم کنید:

```env
MISSION_BOT_TOKEN_TELEGRAM=your_token
MISSION_BOT_TOKEN_BALE=your_token
MISSION_MEDIA_BOT_TOKEN_TELEGRAM=your_token
MISSION_MEDIA_BOT_TOKEN_BALE=your_token
MISSION_APPROVAL_GROUP_CHAT_ID=your_group_chat_id
```

---

## ✅ مرحله 3: آپلود آموزش‌ها با ربات مدیا

1. به **ربات مدیا ماموریت** بروید
2. دستور زیر را ارسال کنید (به جای `1` شناسه ماموریت را بگذارید):
   ```
   /upload_mission_1
   ```
3. سپس فایل‌های آموزشی را یکی یکی ارسال کنید:
   - عکس
   - ویدیو
   - صوت
   - PDF
   - متن

**نکته**: هر فایل به صورت خودکار به ماموریت اضافه می‌شود.

---

## ✅ مرحله 4: ارسال آموزش‌ها به پرسنل

### روش 1: از طریق ربات ماموریت (توصیه می‌شود)

1. پرسنل در **ربات ماموریت** دستور زیر را ارسال می‌کند:
   ```
   /get_training
   ```
2. سیستم به صورت خودکار:
   - Prompt را ارسال می‌کند
   - تمام محتواهای آموزشی را به ترتیب ارسال می‌کند

### روش 2: از طریق ربات مدیا (برای ادمین)

در ربات مدیا:
```
/get_training_1
```

**نکته**: برای ارسال ترتیبی، Queue Worker باید در حال اجرا باشد:
```bash
php artisan queue:work
```

---

## ✅ مرحله 5: انجام ماموریت و ارسال لینک

1. پرسنل آموزش‌ها را مشاهده می‌کند
2. ماموریت را انجام می‌دهد
3. در **ربات ماموریت**، لینک نتیجه را ارسال می‌کند:
   ```
   https://example.com/result/12345
   ```
4. سیستم به صورت خودکار:
   - لینک را ذخیره می‌کند
   - وضعیت را به `pending_approval` تغییر می‌دهد
   - پیام را در گروه تایید ارسال می‌کند

---

## ✅ مرحله 6: تایید یا رد در گروه

1. در **گروه تایید**، پیامی با اطلاعات ماموریت ارسال می‌شود

2. برای **تایید**:
   - به پیام **reply** کنید
   - کلمه **"تایید"** را ارسال کنید
   - سیستم:
     - وضعیت را به `approved` تغییر می‌دهد
     - `approved_by_chat_id` را ثبت می‌کند
     - امتیاز به پرسنل اضافه می‌شود

3. برای **رد**:
   - به پیام **reply** کنید
   - دلیل رد را ارسال کنید
   - سیستم:
     - وضعیت را به `rejected` تغییر می‌دهد
     - `rejection_reason` را ثبت می‌کند

---

## ✅ مرحله 7: بررسی نتایج

### در دیتابیس:

```sql
-- مشاهده ماموریت
SELECT * FROM missions WHERE id = 1;

-- مشاهده پرسنل‌های assign شده
SELECT mp.*, p.first_name, p.last_name 
FROM mission_personnel mp
JOIN personnel p ON mp.personnel_id = p.id
WHERE mp.mission_id = 1;

-- مشاهده تاییدکننده
SELECT approved_by_chat_id, approved_at, status
FROM mission_personnel
WHERE mission_id = 1 AND status = 'approved';
```

---

## 📝 خلاصه دستورات

| مرحله | ربات | دستور | توضیح |
|-------|------|-------|-------|
| 3 | مدیا | `/upload_mission_1` | شروع آپلود آموزش‌ها |
| 3 | مدیا | ارسال فایل | آپلود فایل آموزشی |
| 4 | ماموریت | `/get_training` | دریافت آموزش‌ها (پرسنل) |
| 4 | مدیا | `/get_training_1` | مشاهده لیست آموزش‌ها (ادمین) |
| 5 | ماموریت | ارسال لینک | ارسال نتیجه کار |
| 6 | گروه تایید | Reply + "تایید" | تایید ماموریت |
| 6 | گروه تایید | Reply + دلیل | رد ماموریت |

---

## ⚠️ نکات مهم

1. **Queue Worker**: حتماً باید در حال اجرا باشد:
   ```bash
   php artisan queue:work
   ```

2. **Webhook**: مطمئن شوید webhook‌ها به درستی تنظیم شده‌اند

3. **گروه تایید**: ربات باید در گروه عضو باشد و دسترسی ارسال پیام داشته باشد

4. **لاگ‌ها**: در صورت مشکل، لاگ‌ها را بررسی کنید:
   ```bash
   tail -f storage/logs/laravel.log
   ```

---

## 🔍 عیب‌یابی سریع

| مشکل | راه حل |
|------|--------|
| فایل آپلود نمی‌شود | چک کنید webhook ربات مدیا درست باشد |
| آموزش‌ها ارسال نمی‌شود | Queue Worker را اجرا کنید |
| لینک در گروه ارسال نمی‌شود | `MISSION_APPROVAL_GROUP_CHAT_ID` را چک کنید |
| تایید کار نمی‌کند | مطمئن شوید reply به پیام درست انجام می‌شود |

---

**موفق باشید! 🚀**

