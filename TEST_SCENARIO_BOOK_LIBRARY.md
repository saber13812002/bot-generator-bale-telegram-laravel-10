# سناریوی تست — ربات کتابخانه (Smart Book Library)

> مسیر فایل: `TEST_SCENARIO_BOOK_LIBRARY.md`

---

## پیش‌نیازها

1. ربات **book-library** (اصلی) و **book-library-reader** (کتابخوان) از قبل ساخته شده باشند
2. Nova در دسترس باشد: `http://bots.pardisania.ir/nova`
3. ادمین مادر (شما) در ربات‌ها عضو باشید
4. کش پاک شده باشد: `php artisan cache:clear`

---

## 🧪 سناریوی ۱ — مدیریت دسته‌بندی‌ها و محتوا در Nova

### مرحله ۱-۱: ورود به Nova
1. وارد `http://bots.pardisania.ir/nova/resources/content-categories` شوید
2. ✅ صفحه **🏷 Content Categories** را می‌بینید

### مرحله ۱-۲: ایجاد دسته‌بندی جدید
1. کلیک **Create 🏷 Content Category**
2. در فیلد **Bot** -> ربات book-library خود را انتخاب کنید
3. **Title**: `کتاب‌های صوتی انگیزشی`
4. **Sort Order**: `1`
5. **Active**: ✅ فعال
6. کلیک **Create**
7. ✅ دسته با موفقیت ساخته شد

### مرحله ۱-۳: ایجاد دسته‌بندی دوم
1. دسته جدید با **Title**: `پادکست‌های آموزشی`
2. **Sort Order**: `2`
3. ✅ دسته دوم ساخته شد

### مرحله ۱-۴: ایجاد آیتم محتوا
1. کلیک **Create 📦 Content Item**
2. **Bot**: ربات book-library
3. **Category**: `کتاب‌های صوتی انگیزشی`
4. **Title**: `قدرت عادت — فصل اول`
5. **Queue Order**: `1`
6. **Active**: ✅ فعال
7. کلیک **Create**
8. ✅ آیتم ساخته شد

### مرحله ۱-۵: ایجاد Asset (فایل صوتی)
1. از صفحه آیتم، کلیک **Create 🎬 Content Asset**
2. **Content Item**: (خودکار مقداردهی شده)
3. **Type**: `🎵 Audio`
4. **Content URL**: (اختیاری — اگر فایل در سرور دارید)
5. **Telegram File ID**: (اگر file_id تلگرام دارید)
6. کلیک **Create**
7. ✅ Asset ساخته شد

### مرحله ۱-۶: تنظیم فوتر کپشن
1. برید به **Nova → Bots**
2. ربات book-library خود را باز کنید
3. فیلد **Caption Footer** را پر کنید:
   ```
   📚 کتابخانه هوشمند
   @{bot_name}
   ```
4. کلیک **Update**
5. ✅ فوتر ذخیره شد

---

## 🧪 سناریوی ۲ — تست ربات book-library (اصلی)

### مرحله ۲-۱: استارت ربات
1. ربات book-library را استارت کنید `/start`
2. ✅ پیام خوش‌آمدگویی و منو نمایش داده می‌شود
3. ✅ دکمه‌های `📚 دسته‌ها`, `📖 کتاب‌های من`, `⭐ ارتقا` نمایش داده می‌شود

### مرحله ۲-۲: مشاهده دسته‌بندی‌ها
1. کلیک **📚 دسته‌ها** یا ارسال `/categories`
2. ✅ لیست دسته‌بندی‌ها (کتاب‌های صوتی انگیزشی, پادکست‌های آموزشی) نمایش داده می‌شود

### مرحله ۲-۳: دریافت محتوا
1. روی یک دسته کلیک کنید
2. ✅ پیام "در حال آماده‌سازی..." نمایش داده می‌شود
3. ✅ فایل صوتی (پادکست) ارسال می‌شود
4. ✅ کپشن شامل فوتر تنظیم شده در Nova است

### مرحله ۲-۴: پنل مدیریت (`/manage`)
1. دستور `/manage` را ارسال کنید
2. ✅ پنل مدیریت با آمار نمایش داده می‌شود:
   - تعداد دسته‌بندی‌ها
   - تعداد آیتم‌ها
   - دکمه‌های inline برای مدیریت

### مرحله ۲-۵: مدیریت دسته‌بندی‌ها (`/categories`)
1. دستور `/categories` یا `/tags` را ارسال کنید
2. ✅ لیست دسته‌بندی‌ها با آیکون فعال/غیرفعال و تعداد آیتم‌ها نمایش داده می‌شود

---

## 🧪 سناریوی ۳ — تست ربات کتابخوان (Reader)

### مرحله ۳-۱: استارت ربات کتابخوان
1. ربات book-library-reader را استارت کنید `/start`
2. ✅ پیام خوش‌آمدگویی نمایش داده می‌شود

### مرحله ۳-۲: دریافت محتوا
1. از ربات اصلی book-library یک کتاب را انتخاب کنید
2. ✅ محتوا از طریق ربات کتابخوان ارسال می‌شود
3. ✅ دکمه‌های Quick Action (PDF, اینفوگرافی, پخش مجدد) نمایش داده می‌شود

---

## 🧪 سناریوی ۴ — دستورات ادمین مادر

### مرحله ۴-۱: ارسال پیام به کاربر (`/messagetothischatid`)
1. در ربات book-library یا reader، ارسال کنید:
   ```
   /messagetothischatid CHAT_ID سلام این یک پیام تست است
   ```
   (به جای CHAT_ID، آیدی عددی کاربر مقصد را وارد کنید)
2. ✅ پیام "✅ پیام به CHAT_ID ارسال شد." نمایش داده می‌شود
3. ✅ کاربر مقصد پیام "📨 پیام از ادمین: ..." را دریافت می‌کند

---

## 🧪 سناریوی ۵ — ارسال ساعتی (Scheduled)

### مرحله ۵-۱: تست خشک (Dry-run)
```bash
php artisan content:deliver-hourly --dry-run
```
1. ✅ خروجی نشان می‌دهد برای کدام کاربران و کدام آیتم‌ها ارسال می‌شود
2. ✅ پیام واقعی ارسال نمی‌شود

### مرحله ۵-۲: تست با ربات خاص
```bash
php artisan content:deliver-hourly --dry-run --bot-id=5
```
(شماره Bot ID ربات book-library خود را وارد کنید)
1. ✅ فقط همان ربات بررسی می‌شود

### مرحله ۵-۳: تست واقعی (با احتیاط)
```bash
php artisan content:deliver-hourly
```
1. ✅ محتوا به کاربرانی که بیش از ۱ ساعت از آخرین دریافتشان گذشته ارسال می‌شود
2. ✅ اگر محتوایی در صف نباشد، پیام "📭 محتوای جدیدی در دسته ... وجود ندارد" به کاربر می‌رسد
3. ✅ ادمین مادر پیام هشدار دریافت می‌کند

### مرحله ۵-۴: تست با دسته خاص
```bash
php artisan content:deliver-hourly --dry-run --category-id=1
```
1. ✅ فقط دسته مورد نظر بررسی می‌شود

---

## 🧪 سناریوی ۶ — تست `/adminkie`

### مرحله ۶-۱: کاربر عادی دستور adminkie می‌زند
1. کاربری که ادمین نیست در ربات book-library `/adminkie` بزند
2. ✅ پیام "درخواست شما ثبت شد" نمایش داده می‌شود
3. ✅ ادمین مادر پیام اعلان با مشخصات کامل دریافت می‌کند:
   - 🤖 نام ربات
   - 🆔 Bot ID
   - 👤 نام درخواست‌دهنده
   - 💬 Chat ID
   - 📱 Origin
   - 🔗 Username
   - 📡 Endpoint

### مرحله ۶-۲: تایید درخواست adminkie
1. ادمین مادر دستور `/adminbot_confirm REQUEST_ID BOT_ID` را ارسال کند
2. ✅ کاربر پیام "شما به عنوان ادمین تایید شدید" دریافت می‌کند
3. ✅ مالکیت ربات برای کاربر ثبت می‌شود

---

## 🧪 سناریوی ۷ — تست ارسال فایل توسط ادمین ربات

### مرحله ۷-۱: ارسال فایل صوتی
1. ادمین ربات (کسی که با `/adminkie` تایید شده) یک فایل صوتی به ربات بفرستد
2. ✅ پیام "فایل دریافت شد" نمایش داده می‌شود
3. ✅ دستور `/addFileToCategory PENDING_ID` برای انتساب به دسته نمایش داده می‌شود

### مرحله ۷-۲: انتساب فایل به دسته
1. ادمین دستور `/addFileToCategory 1` را ارسال کند
2. ✅ لیست دسته‌بندی‌ها نمایش داده می‌شود
3. ادمین روی یک دسته کلیک کند
4. ✅ پیام "فایل به دسته X اضافه شد با ترتیب Y" نمایش داده می‌شود

---

## 🧪 سناریوی ۸ — تست Broadcast

### مرحله ۸-۱: ارسال همگانی
1. ادمین ربات دستور `/broadcast` را ارسال کند
2. ✅ ربات درخواست "متن پیام همگانی را وارد کنید" را نشان می‌دهد

### مرحله ۸-۲: انتخاب فیلتر
1. ادمین متن پیام را وارد کند
2. ✅ گزینه‌های فیلتر (همه, رایگان, ویژه, فعال) نمایش داده می‌شود
3. ادمین یکی را انتخاب کند
4. ✅ پیام تایید با جزئیات نمایش داده می‌شود
5. ادمین تایید کند
6. ✅ پیام "ارسال همگانی در صف قرار گرفت" نمایش داده می‌شود

---

## 🧪 سناریوی ۹ — تست Caption Footer در Nova

### مرحله ۹-۱: تنظیم فوتر
1. در Nova → Bot → ربات book-library
2. فیلد Caption Footer را پر کنید با استفاده از متغیرها:
   ```
   🌐 کانال ما: https://ble.ir/my_channel
   📱 ربات ما: @{bot_name} در بله و تلگرام
   ```
3. کلیک Update

### مرحله ۹-۲: بررسی ارسال
1. یک کاربر از ربات محتوا بگیرد
2. ✅ انتهای کپشن فایل صوتی، فوتر تنظیم شده را نشان می‌دهد
3. ✅ `{bot_name}` با نام واقعی ربات جایگزین شده است

---

## 🧪 سناریوی ۱۰ — بررسی اتمام صف

### مرحله ۱۰-۱: مصرف تمام آیتم‌های یک دسته
1. کاربر به تعداد آیتم‌های یک دسته محتوا بگیرد تا تمام شوند
2. ✅ وقتی آیتم بعدی نباشد، پیام مناسب نمایش داده می‌شود

### مرحله ۱۰-۲: اجرای Schedule بعد از اتمام
1. اجرا: `php artisan content:deliver-hourly --dry-run`
2. ✅ در خروجی نشان می‌دهد که برای آن کاربر محتوایی وجود ندارد
3. اگر اجرای واقعی باشد، به کاربر و ادمین نوتیف می‌دهد

---

## 🧪 سناریوی ۱۱ — چک‌لیست نهایی

- [ ] Nova: ایجاد دسته‌بندی (Content Category)
- [ ] Nova: ایجاد آیتم محتوا (Content Item)
- [ ] Nova: ایجاد Asset صوتی (Content Asset)
- [ ] Nova: تنظیم Caption Footer روی ربات
- [ ] ربات: استارت و نمایش منو
- [ ] ربات: دریافت محتوا از دسته
- [ ] ربات: دکمه‌های Quick Action
- [ ] ربات: پنل `/manage`
- [ ] ربات: `/categories` یا `/tags`
- [ ] ربات: `/messagetothischatid`
- [ ] Schedule: `--dry-run` بدون خطا
- [ ] Schedule: ارسال واقعی
- [ ] Schedule: نوتیف اتمام صف
- [ ] فوتر کپشن در انتهای پیام
- [ ] `/adminkie` ثبت درخواست
- [ ] `/adminkie` تایید توسط ادمین
